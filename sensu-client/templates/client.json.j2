{
  "client": {
    "name": "{{inventory_hostname}}",
    "address": "{{ansible_ssh_host}}",
    "subscriptions": [ "all", "system"{{sensu_subscriptions}} ],
    "keepalive": {
      "handlers": [ "hubot", "elasticsearch_handler" ]
    }
  }
}
